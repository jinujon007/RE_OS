[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: pure Python, no DB, no network
    integration: requires live PostgreSQL DB (auto-skipped via socket check when DB unreachable)
    test_id(ID): traceability marker linking a test to its GATE criterion (e.g. A1, B3)
