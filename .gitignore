# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Environment
.env

# dbt compiled output + logs
dbt/target/
dbt/logs/
dbt/dbt_packages/
.env.*
!.env.example

# Logs & outputs (don't commit run data)
logs/
outputs/
# BERTScore reference corpus files (manually selected — not auto-generated, but not source)
outputs/references/*.txt
kilo_output/
kilo_logs/
kilo/
prefect_flows/

# Docker volumes
postgres_data/
ollama_data/

# Training data (RERA extraction model — never commit real project data)
data/training/

# Model artifacts (large binary files — GGUF, checkpoints)
models/*.gguf
models/rera-extractor-3b/
models/rera-extractor-3b-merged/
models/gguf_export_info.json

# ChromaDB persistent data (generated at runtime — not source)
chroma_data/
# Local chroma data dir created when running outside Docker
data/chroma/

# VS Code (keep .vscode committed so team shares settings)
# .vscode/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Obsidian local state
.obsidian/

# Claude Code local permissions (project settings.json is committed; local overrides are not)
.claude/settings.local.json

# Aider AI chat history (local dev tool logs — not source)
.aider.chat.history.md
.aider.input.history

# Kilo Code extension workspace (local dev tool state — not source)
.kilo/
# Kilo temp/debug scripts (never commit these)
_tmp_*.py

# Runtime crew output files (these are logs, not source)
crew_error.txt
crew_run.txt

# pytest
.pytest_cache/
.coverage
htmlcov/
.gstack/
.aider*

# Temporary debug/test scripts (never source)
tmp_*.py
validate_renderer.py

# Sprint 37 — synthetic eval images (generated, not source)
data/eval/

# Backup files
*.bak

# ── PRIVATE CONTEXT ── not for public repo ────────────────────────────────────
# AI assistant instruction files (contain private LLS context, system prompts)
CLAUDE.md
BRAIN_BRIEF.md

# Development tracking — internal sprint/task management
NEXT_TASKS.md
TASK_QUEUE.md
TASK_BRIEFS.md
TASK_ARCHIVE.md
TODOS.md
CURRENT_TASK.md
DISPATCH.md
GOVERNANCE.md
REPLAN.md
STRATEGY.md
SUPPORT.md
BEGINNER_GUIDE.md

# ── DEV TOOLING ── local workflow only, not product source ────────────────────
.claude/
.cline_logs/
.clinerules
.kilocoderules
.compound-engineering/
kilo_logs/
kilo_output/
scripts/
plans/
test-results/

AGENTS.md
task.md
DEVLOG.md
TOOL_GUIDE.md
OPERATIONAL_REVIEW.md
CLINE_BRIEF.md
KILO_BRIEF.md

# ── IDE & LOCAL ENVIRONMENT ───────────────────────────────────────────────────
.vscode/

# ── OBSIDIAN VAULT BLEED ── vault notes that don't belong in product repo ─────
# Obsidian uses private-use-area unicode for path separators (U+F03A, U+F05C)
# — these patterns catch vault files accidentally visible from this working dir
*.canvas
RE_OS Home.md
01 Daily/

# ── CODE INTELLIGENCE INDEX ── generated at runtime ──────────────────────────
.codegraph/
