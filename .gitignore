# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Environment
.env

# dbt compiled output + logs
dbt/target/
dbt/logs/
dbt/dbt_packages/
.env.*
!.env.example

# Logs & outputs (don't commit run data)
logs/
outputs/
# BERTScore reference corpus files (manually selected — not auto-generated, but not source)
outputs/references/*.txt
kilo_output/
kilo_logs/

# Docker volumes
postgres_data/
ollama_data/

# Training data (RERA extraction model — never commit real project data)
data/training/

# Model artifacts (large binary files — GGUF, checkpoints)
models/*.gguf
models/rera-extractor-3b/
models/rera-extractor-3b-merged/
models/gguf_export_info.json

# ChromaDB persistent data (generated at runtime — not source)
chroma_data/
# Local chroma data dir created when running outside Docker
data/chroma/

# VS Code (keep .vscode committed so team shares settings)
# .vscode/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Obsidian local state
.obsidian/

# Claude Code local permissions (project settings.json is committed; local overrides are not)
.claude/settings.local.json

# Aider AI chat history (local dev tool logs — not source)
.aider.chat.history.md
.aider.input.history

# Kilo Code extension workspace (local dev tool state — not source)
.kilo/

# Runtime crew output files (these are logs, not source)
crew_error.txt
crew_run.txt

# pytest
.pytest_cache/
.coverage
htmlcov/
.gstack/
.aider*

# Temporary debug/test scripts (never source)
tmp_*.py
validate_renderer.py

# Sprint 37 — synthetic eval images (generated, not source)
data/eval/

# Backup files
*.bak

# Internal ops docs (private LLS context — not for public repo)
BRAIN_BRIEF.md
NEXT_TASKS.md
plans/(CAI)*

# Dev tooling — local workflow only, not product source
.claude/
.cline_logs/
kilo_logs/
scripts/
plans/
AGENTS.md
TASK_QUEUE.md
task.md
DEVLOG.md
TOOL_GUIDE.md
OPERATIONAL_REVIEW.md
.clinerules
CLINE_BRIEF.md
CURRENT_TASK.md
KILO_BRIEF.md
TASK_ARCHIVE.md

# Compound Engineering local config (machine-specific, not shared)
.compound-engineering/*.local.yaml

# VS Code personal settings (contains local paths and credentials)
.vscode/

# Obsidian notes (vault files that don't belong in product repo)
*.canvas
RE_OS Home.md

# Code intelligence index (generated at runtime by codegraph — not source)
.codegraph/

# Vault daily logs that bleed in from parent directory when running Claude Code
"01 Daily/"
01 Daily/
