# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.env.*
!.env.example

# Logs & outputs (don't commit run data)
logs/
outputs/
kilo_output/
kilo_logs/

# Docker volumes
postgres_data/
ollama_data/

# VS Code (keep .vscode committed so team shares settings)
# .vscode/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Obsidian local state
.obsidian/

# Claude Code local permissions (project settings.json is committed; local overrides are not)
.claude/settings.local.json

# Aider AI chat history (local dev tool logs — not source)
.aider.chat.history.md
.aider.input.history

# Kilo Code extension workspace (local dev tool state — not source)
.kilo/

# Runtime crew output files (these are logs, not source)
crew_error.txt
crew_run.txt

# pytest
.pytest_cache/
.coverage
htmlcov/
.gstack/
.aider*

# Temporary debug/test scripts (never source)
tmp_*.py

# Backup files
*.bak

# Internal ops docs (private LLS context — not for public repo)
BRAIN_BRIEF.md
NEXT_TASKS.md
plans/(CAI)*

# Dev tooling — local workflow only, not product source
.claude/
.cline_logs/
kilo_logs/
scripts/
plans/
AGENTS.md
TASK_QUEUE.md
task.md
DEVLOG.md
TOOL_GUIDE.md
OPERATIONAL_REVIEW.md
.clinerules

# VS Code personal settings (contains local paths and credentials)
.vscode/

# Obsidian notes (vault files that don't belong in product repo)
*.canvas
RE_OS Home.md
