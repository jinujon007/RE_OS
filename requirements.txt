# Multi-agent orchestration
crewai==0.80.0
crewai-tools==0.17.0

# LLM integrations — CrewAI 0.80+ uses LiteLLM internally (no LangChain needed)
# litellm is used directly (success_callback, completion) — pin to avoid silent breaking changes
litellm>=1.40.0,<2.0.0
openai>=1.50.0,<2.0.0
groq>=0.11.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.35
geoalchemy2==0.20.0

# Web scraping
requests==2.32.3
beautifulsoup4==4.14.3
lxml>=6.0.2                 # Bumped from 5.3.0 — scrapling requires lxml>=6.0.2
playwright>=1.56.0,<2.0.0           # scrapling[fetchers]>=0.4.0 requires playwright>=1.56.0
httpx[http2]==0.28.1
# Scrapling: TLS fingerprint spoofing (Fetcher) + stealth Playwright (DynamicFetcher)
# Replaces plain requests for bot-protected portals. No scrapling install needed —
# DynamicFetcher reuses the Playwright Chromium already at PLAYWRIGHT_BROWSERS_PATH.
scrapling[fetchers]>=0.4.0

# Task queue
redis==7.4.0
rq==1.16.1

# Scheduling
apscheduler==3.11.2

# Data processing
pandas==3.0.3
numpy==2.4.6

# Geo utilities
shapely==2.1.2
pyproj==3.7.0
geopandas>=0.14.0,<1.0.0
osmnx>=1.9.0,<2.0.0
pysal>=23.1.0,<25.0.0
pandana>=0.7.0,<1.0.0
folium>=0.16.0

# Financial risk metrics (Tier 3 — deferred until T-720/T-767 sprint)
# empyrical + pyfolio: Sharpe ratio, max drawdown, portfolio correlation
# Requires: >=6 months of market_snapshots data + active deal_pipeline entries
# Add back when T-762 (PSF forecasting) sprint begins
# empyrical>=0.5.5
# pyfolio>=0.9.0

# Time-series forecasting (Tier 3 — deferred until T-762 sprint)
# mlforecast: LightGBM-backed PSF forecast. Requires >=6 months project_snapshots.
# Earliest viable sprint: after 2026-12-01 (6 months post GATE-56)
# mlforecast>=0.13.0

# Utilities
python-dotenv==1.2.2
pydantic==2.9.2
loguru==0.7.2
tenacity>=8.1.0

# Terminal output — capped by embedchain transitive dep (rich<14.0.0)
rich>=13.7.0,<14.0.0
# Chromadb capped by embedchain transitive dep
chromadb>=0.5.10,<0.6.0

# Future: workflow orchestration
# prefect>=2.0.0

alembic>=1.13.0

# dbt — SQL transformation layer (Tier 2)
dbt-core>=1.7.0
dbt-postgres>=1.7.0

# Great Expectations — data quality checks (Tier 2)
great-expectations>=0.18.0

# Dashboard (v2 — FastAPI)
prometheus-client>=0.21.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
slowapi>=0.1.9

# Scout utilities (portal + news extraction)
price-parser>=0.3.4
dateparser>=1.2.0

# PDF extraction (MIT — machine-readable RERA PDFs, sale deeds, encumbrance certs)
pdfplumber>=0.11.0

# Document → Markdown conversion (PDF, DOCX, XLSX, PPTX, HTML, images, etc.)
# Used by: Legal Head (EC PDFs), RERA detail scout (approval docs), IGR (Excel downloads)
markitdown>=0.1.0

# Evaluation — ROUGE quality metrics for analyst briefs (no torch required)
rouge-score>=0.1.2

# HF embeddings + reranking + evaluation (Sprints 32–37)
sentence-transformers>=2.7.0  # semantic dedup + cross-encoder reranker
evaluate>=0.4.0  # BERTScore weekly eval

# ── Sprint 36 — HF RERA Extractor (QLoRA Fine-Tune) ──────────────────────────
# Training: pip install these in the agents container for GPU fine-tuning
# torch>=2.1.0            # CUDA 12.1+ required for RTX 3050 4GB
# transformers>=4.38.0     # Qwen2.5 support
# accelerate>=0.27.0       # device_map="auto"
# peft>=0.9.0              # QLoRA (LoRAConfig, get_peft_model)
# trl>=0.8.0               # SFTTrainer
# bitsandbytes>=0.43.0     # 4-bit quantization (nf4)
# datasets>=2.18.0         # HuggingFace Dataset wrapper
# llama-cpp-python>=0.2.0  # GGUF export (CUDA-enabled)
datasets>=2.18.0  # eval harness

# Testing
pytest>=7.0
pytest-mock>=3.0
pytest-cov>=4.0